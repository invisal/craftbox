# Normalize all text files to LF on checkout, regardless of the contributor's
# local core.autocrlf setting. Without this, Windows checkouts with
# autocrlf=true get CRLF line endings while prettier (endOfLine: lf by default)
# expects LF, causing every line of every file to fail `prettier/prettier`.
* text=auto eol=lf
