/**
 * Global row height for KubeTable (in pixels).
 * Adjust this single value to change the height of all table rows across the kuberneter tool.
 * It controls both the visual row height (via inline style) and the virtualization scroll math.
 */
export const ROW_HEIGHT = 30;
